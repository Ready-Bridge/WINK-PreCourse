// 오늘 날짜로 바꾸기

const reloadCount = () => {
  // 미완료 개수 업데이트
};
reloadCount(); // 첫 화면 로딩 시 바로 개수 업데이트를 위해서 함수 실행

const delTodo = (el) => {
  // 투두 삭제 후 개수 업데이트 필요
};

const todoDone = (el) => {
  // 투두 완료 후 개수 업데이트 필요
};

const addTodo = () => {
  // 투두 추가 후 개수 업데이트 필요, 텍스트박스가 빈칸이면 알림 띄우기
};
